import Hero from '../components/Hero';
import { StatsRow } from '../components/StatsRow';

const HeroSection = () => {
  return (
    <>
      <Hero />
      <StatsRow />
    </>
  );
};

export default HeroSection;
